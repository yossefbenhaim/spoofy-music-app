export enum SliceName {
  currentUser = 'currentUser',
  currentPlaylist = 'currentPlaylist',
  songs = 'songs',
  artist = 'artist',
  favorites = 'favorites',
  playlists = 'playlists',
  users = 'users',
  themeMode = 'themeMode',
  auth = 'auth',
}

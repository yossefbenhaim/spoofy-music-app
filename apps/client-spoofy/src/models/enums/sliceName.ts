export enum SliceName {
    currentUser = 'currentUser',
    currentPlaylist = 'currentPlaylist',
    songs = 'songs',
    favorites = 'favorites',
    playlists = 'playlists',
    users = 'users',
    themeMode = 'themeMode',
}

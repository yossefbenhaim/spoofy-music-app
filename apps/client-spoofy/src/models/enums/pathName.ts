export enum PathName {
  songs = 'songs',
  favorites = 'favorites',
  playlist = 'playlist',
  users = 'users',
  settings = 'settings',
  forgotPassword = 'forgotPassword',
  library = '/library/',
  login = '/',
}
